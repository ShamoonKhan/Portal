module CoursesHelper
#     def teacher_name(course)
#          user_id = UserCourse.find_by(course_id: x.id).user_id 
#       if user_id.present?
#       User.find(user_id).first_name  User.find(user_id).lname 
#     end
# end
end